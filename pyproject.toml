[project]
name = "maybech"
version = "0.1.0"
description = "Signal-driven OKX strategy and logical position manager"
requires-python = ">=3.11,<3.15"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
